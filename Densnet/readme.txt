里面baidu.py是数据的准备包括读取数据，原本是图片数据，但我利用了pickel进行了序列化处理，将原来的所有图片进行了序列化，原图100x100大小，3通道。序列化时，每一行代表一副图片，30000个数据分别代表图像的rgb三个通道的像素（各10000）。所有训练图片和标签都被序列化后存储在train.pkl里面。由于文件太大，这里没有给出数据。但其实可以自己写出将图片序列化的代码，并不复杂。
Densnet_baidu.py就是模型建立和训练的文件，运行该文件即可，运行过后还将会产生log.txt ;logs; model文件，分别记录了训练的过程和模型。
